# 依赖与构建产物（Dockerfile 内自己 npm ci + build）
node_modules
dist

# git / 编辑器 / 工具元数据
.git
.gitignore
.gitattributes
.vscode
.idea

# 项目内的开发工作流目录
.claude

# wrangler / 本地环境变量（不该进镜像）
.wrangler
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example

# dev-only 配置
dev-proxy.config.json

# 测试与覆盖率
coverage
*.test.ts
*.test.tsx
*.test.mjs
vitest.config.*

# 日志 / 临时文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker 配置自身没必要进镜像
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.*.yml
Caddyfile
cors-proxy.conf

# README 不影响构建，留它进去也行；本项目不强制排除。
